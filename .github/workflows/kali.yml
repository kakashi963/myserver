name: Kali

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: ubuntu-latest
    timeout-minutes: 3600
    container:
      image: kalilinux/kali-rolling
      options: --privileged

    steps:
      - name: Install Dependencies
        run: |
          export DEBIAN_FRONTEND=noninteractive
          apt-get update -y

          # Install Kali GNOME desktop with full toolset
          apt-get install -y \
            kali-desktop-gnome \
            kali-linux-default \
            xrdp \
            dbus dbus-x11 \
            curl wget sudo ca-certificates gnupg \
            procps net-tools iproute2

          # Add Tailscale repository
          curl -fsSL https://pkgs.tailscale.com/stable/debian/bookworm.noarmor.gpg \
            | tee /usr/share/keyrings/tailscale-archive-keyring.gpg > /dev/null

          curl -fsSL https://pkgs.tailscale.com/stable/debian/bookworm.tailscale-keyring.list \
            | tee /etc/apt/sources.list.d/tailscale.list

          apt-get update -y
          apt-get install -y tailscale

      - name: Configure Core RDP Settings
        run: |
          # Generate machine-id
          dbus-uuidgen | tee /etc/machine-id /var/lib/dbus/machine-id

          # Allow root to run X
          echo "allowed_users=anybody" > /etc/X11/Xwrapper.config

          # Set GNOME session for RDP
          echo "export XDG_SESSION_TYPE=x11" > /root/.xsession
          echo "export GNOME_SHELL_SESSION_MODE=kali" >> /root/.xsession
          echo "exec gnome-session" >> /root/.xsession
          chmod +x /root/.xsession

          # Fix xrdp config
          sed -i 's/port=3389/port=3389/g' /etc/xrdp/xrdp.ini
          # Use Xorg backend for better GNOME compatibility
          sed -i 's/use_vsock=true/use_vsock=false/g' /etc/xrdp/xrdp.ini

          # Create required xrdp dirs
          mkdir -p /var/run/xrdp /var/log/xrdp
          chmod 755 /var/run/xrdp

          # Start xrdp
          /usr/sbin/xrdp-sesman --nodaemon &
          sleep 2
          /usr/sbin/xrdp --nodaemon &
          sleep 3

          echo "=== xrdp process check ==="
          ps aux | grep xrdp
          echo "=== port 3389 check ==="
          ss -tlnp | grep 3389 || echo "Port 3389 not open yet"

      - name: Create RDP User with Secure Password
        run: |
          PASSWORD=$(cat /dev/urandom | tr -dc 'A-Za-z0-9!@#$%^&*' | head -c 16)

          useradd -m -s /bin/bash RDP
          echo "RDP:$PASSWORD" | chpasswd
          usermod -aG sudo RDP

          # Set GNOME session for RDP user
          echo "export XDG_SESSION_TYPE=x11" > /home/RDP/.xsession
          echo "exec gnome-session" >> /home/RDP/.xsession
          chmod +x /home/RDP/.xsession
          chown RDP:RDP /home/RDP/.xsession

          echo "RDP_CREDS=User: RDP | Password: $PASSWORD" >> $GITHUB_ENV

          id RDP || { echo "User creation failed"; exit 1; }

      - name: Establish Tailscale Connection
        run: |
          tailscaled --state=/tmp/tailscaled.state &
          sleep 3

          tailscale up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} \
                       --hostname=gh-runner-$GITHUB_RUN_ID

          TS_IP=""
          RETRIES=0
          while [ -z "$TS_IP" ] && [ $RETRIES -lt 10 ]; do
            TS_IP=$(tailscale ip -4 2>/dev/null || true)
            sleep 5
            RETRIES=$((RETRIES + 1))
          done

          if [ -z "$TS_IP" ]; then
            echo "Tailscale IP not assigned. Exiting."
            exit 1
          fi

          echo "TAILSCALE_IP=$TS_IP" >> $GITHUB_ENV

      - name: Verify RDP Accessibility
        run: |
          echo "Tailscale IP: $TAILSCALE_IP"

          if ! ss -tlnp | grep -q 3389; then
            echo "xrdp not on port 3389, attempting restart..."
            /usr/sbin/xrdp-sesman --nodaemon &
            sleep 2
            /usr/sbin/xrdp --nodaemon &
            sleep 3
          fi

          if (echo > /dev/tcp/$TAILSCALE_IP/3389) 2>/dev/null; then
            echo "TCP connectivity successful!"
          else
            echo "WARNING: Port 3389 not reachable via Tailscale yet, continuing..."
          fi

      - name: Maintain Connection
        run: |
          echo ""
          echo "=== RDP ACCESS ==="
          echo "Address : $TAILSCALE_IP"
          echo "Username: RDP"
          echo "Password: $RDP_CREDS"
          echo "=================="
          echo ""

          while true; do
            echo "[$(date)] RDP Active - Cancel workflow to terminate"
            ss -tlnp | grep -q 3389 || (/usr/sbin/xrdp --nodaemon & sleep 2)
            sleep 300
          done
